import { Dimensions, StyleSheet } from 'react-native';
import { C } from '../colors';

const { width: screenW } = Dimensions.get('window');
const COLUMNS = 4;
const PADDING = 8;
const GAP = 8;
export const CARD_SIZE = Math.floor((screenW - PADDING * 2 - GAP * (COLUMNS - 1)) / COLUMNS);

export default StyleSheet.create({
  container: { flex: 1, backgroundColor: '#f7f8fa' },
  stickyHeader: {
    backgroundColor: C.surface,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.07,
    shadowRadius: 8,
    elevation: 4,
  },
  titleRow: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
    paddingHorizontal: 16,
    paddingTop: 14,
    paddingBottom: 10,
  },
  backBtn: {
    width: 36, height: 36, borderRadius: 18,
    alignItems: 'center', justifyContent: 'center',
  },
  pageTitle: { fontSize: 17, fontWeight: '700', color: C.text },
  searchWrap: {
    flexDirection: 'row',
    alignItems: 'center',
    marginHorizontal: 16,
    marginBottom: 10,
    paddingHorizontal: 14,
    height: 40,
    backgroundColor: '#f3f4f6',
    borderRadius: 999,
    gap: 8,
  },
  searchInput: { flex: 1, fontSize: 13, color: C.text },
  filterRow: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 16,
    paddingBottom: 12,
  },
  catTabsScroll: { flex: 1 },
  catTab: {
    paddingHorizontal: 12, paddingVertical: 5, borderRadius: 999,
    borderWidth: 1.5, borderColor: C.border,
    backgroundColor: 'transparent', marginRight: 6,
  },
  catTabActive: { backgroundColor: '#E67E5E', borderColor: '#E67E5E' },
  catTabText: { fontSize: 12, fontWeight: '600', color: C.textMuted },
  catTabTextActive: { color: '#fff' },
  catalog: { flex: 1 },
  catalogContent: { padding: PADDING, gap: GAP, paddingBottom: 80 },
  catalogRow: { gap: GAP },
  catalogCard: {
    width: CARD_SIZE,
    backgroundColor: '#fff',
    borderWidth: 1.5,
    borderColor: C.border,
    borderRadius: 16,
    overflow: 'hidden',
  },
  catalogCardAdded: { borderColor: '#E67E5E' },
  catalogImgWrap: {
    width: '100%',
    aspectRatio: 1,
    backgroundColor: '#f9fafb',
    alignItems: 'center',
    justifyContent: 'center',
    borderBottomWidth: 1,
    borderBottomColor: C.border,
  },
  catalogImgWrapAdded: { backgroundColor: '#fff5f2' },
  catalogEmoji: { fontSize: 26 },
  catalogBody: { padding: 5, gap: 2 },
  catalogName: { fontSize: 10, fontWeight: '700', color: C.text },
  catalogCatTag: {
    fontSize: 9, fontWeight: '600',
    paddingHorizontal: 4, paddingVertical: 1,
    borderRadius: 999, alignSelf: 'flex-start',
  },
  checkBadge: {
    position: 'absolute', top: 6, right: 6,
    width: 20, height: 20, backgroundColor: '#E67E5E',
    borderRadius: 10, alignItems: 'center', justifyContent: 'center',
  },
  checkText: { fontSize: 11, fontWeight: '700', color: '#fff' },
  emptyText: { textAlign: 'center', padding: 40, color: C.textMuted, fontSize: 14 },
  selectedBar: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    paddingHorizontal: 16,
    paddingVertical: 12,
    backgroundColor: '#fff',
    borderTopWidth: 1,
    borderTopColor: C.border,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: -4 },
    shadowOpacity: 0.08,
    shadowRadius: 16,
    elevation: 8,
  },
  selectedLabel: { fontSize: 14, fontWeight: '700', color: C.text },
  confirmBtn: {
    paddingHorizontal: 20, paddingVertical: 9,
    backgroundColor: '#E67E5E', borderRadius: 999,
  },
  confirmBtnText: { fontSize: 13, fontWeight: '700', color: '#fff' },
  sheetOverlay: { flex: 1, backgroundColor: 'rgba(0,0,0,0.4)', justifyContent: 'flex-end' },
  detailSheet: {
    backgroundColor: '#fff',
    borderRadius: 20,
    paddingHorizontal: 20,
    paddingTop: 12,
    paddingBottom: 24,
    gap: 16,
    alignItems: 'center',
  },
  sheetHandle: { width: 36, height: 4, backgroundColor: '#e5e7eb', borderRadius: 2, marginBottom: 4 },
  sheetHeader: { width: '100%', flexDirection: 'row', alignItems: 'center', gap: 14 },
  sheetEmoji: { fontSize: 36 },
  sheetName: { fontSize: 17, fontWeight: '700', color: C.text },
  sheetCat: { fontSize: 12, color: C.textMuted, marginTop: 2 },
  sheetRow: {
    width: '100%', flexDirection: 'row',
    alignItems: 'center', justifyContent: 'space-between',
  },
  sheetLabel: { fontSize: 14, fontWeight: '600', color: C.text },
  countRow: { flexDirection: 'row', alignItems: 'center', gap: 16 },
  countBtn: {
    width: 38, height: 38, borderRadius: 19,
    borderWidth: 1.5, borderColor: C.border, backgroundColor: C.bg,
    alignItems: 'center', justifyContent: 'center',
  },
  countBtnText: { fontSize: 22, color: C.text, lineHeight: 26 },
  countDisplay: { fontSize: 20, fontWeight: '700', minWidth: 36, textAlign: 'center', color: C.text },
  expiryGroup: { width: '100%', alignItems: 'center', gap: 10 },
  expiryLabel: { alignSelf: 'flex-start', fontSize: 14, fontWeight: '600', color: C.text },
  dateInput: {
    width: '75%', height: 46, paddingHorizontal: 16,
    borderWidth: 1.5, borderColor: C.border,
    borderRadius: 12, fontSize: 14, color: C.text,
    textAlign: 'center', backgroundColor: C.surface,
  },
  shortcutsRow: { flexDirection: 'row', gap: 7, flexWrap: 'wrap', justifyContent: 'center' },
  shortcutBtn: {
    height: 32, paddingHorizontal: 13, borderRadius: 999,
    borderWidth: 1.5, borderColor: C.border, backgroundColor: C.surface,
    alignItems: 'center', justifyContent: 'center',
  },
  shortcutBtnText: { fontSize: 12, fontWeight: '700', color: C.textSub },
  sheetActions: { width: '100%', flexDirection: 'row', gap: 10, marginTop: 8 },
  cancelBtn: {
    height: 46, paddingHorizontal: 18, borderWidth: 1.5, borderColor: C.border,
    borderRadius: 12, backgroundColor: 'transparent',
    alignItems: 'center', justifyContent: 'center', flexShrink: 0,
  },
  cancelBtnText: { fontSize: 14, fontWeight: '700', color: C.textMuted },
  doneBtn: {
    flex: 1, height: 46, borderRadius: 12, backgroundColor: C.primary,
    alignItems: 'center', justifyContent: 'center',
  },
  doneBtnText: { fontSize: 14, fontWeight: '700', color: '#fff' },
});
